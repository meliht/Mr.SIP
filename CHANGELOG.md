# Changelog

Newest first. `1.2.0` (the modular refactor / initial hardening pass) has its own detailed table of contents at the start of that section, since it's the largest single entry.

## 1.6.8 — F49: SIP-NES's summary count was a probe-success count, not a distinct-live-host count

Found during an end-to-end review pass (source + docs read in full, then every module driven live - both against a throwaway local mock SIP responder and against the project's own Docker Asterisk lab, `mrsip-test-pbx`, `andrius/asterisk:latest`/PJSIP - rather than just re-reading the code).

**The bug**: `nes.py::run()`'s bulk path computed `counter = len(found)`, where `found` is the list `run_worker_pool()` returns from `_scan_one()` - which returns the target host string once per *successful probe*, not once per *distinct live host*. Reproduced live: a single host (the Docker lab, `127.0.0.1:5060`) probed with a `--mt=register` scan (register/subscribe never collapse the identity wordlist down from the bundled 9000-line default - see F20's own reasoning, `NES_USAGE` in `src/cli.py`) answered all 9000 REGISTER probes successfully, and the console summary reported **"9000 live IP address(es) found"** - while `-i`'s own output file, deduplicated separately inside `printResult()`'s `_written_targets_cache` (see F27), correctly held exactly one line for that one host the whole time. The two numbers silently disagreed, and the wrong one was the one printed at the end of every run. Not limited to register/subscribe: the same inflation hits any `--mt` where the operator supplies an explicit multi-entry `--from`/`--to` (the tool's own documented "identity-aware probing" feature, `_resolve_user_pairs()`) against a single target, or a range/CIDR scan where a live host happens to answer more than one identity pair.

**The fix**: `counter = len(set(found))` in both places the bulk path computes it (the normal-completion path and the `KeyboardInterrupt` partial-results path) - `found`'s entries are plain host strings, so deduplicating them directly gives the actual distinct-live-host count the summary line already claims to report. The single-target/single-identity synchronous path (`counter = 1 if found is not None else 0`) was already correct and untouched.

**Verified live**, not just re-run against the fix's own new unit test: re-ran the exact reproduction (`--nes --tn=127.0.0.1 --dp=5060 --mt=register` against the real `mrsip-test-pbx` Asterisk/PJSIP lab, bundled default wordlist) after the fix - summary now correctly reports **"1 live IP address(es) found"**, matching the single line in the `-i` output file.

3 new regression tests (`tests/test_nes.py::TestNesRunSingleProbe`): `test_bulk_probe_counter_dedupes_by_host_not_by_probe` (confirmed it fails against the pre-fix code with the exact reproduced symptom - asserts `"3 live IP address(es) found"` is *not* logged for 3 identity pairs against one host; failed with that literal string present before the fix, passes after), `test_bulk_probe_counter_still_counts_multiple_distinct_hosts` (guards against the `set()` fix overcorrecting - 2 genuinely distinct hosts must still count as 2, not collapse to 1), and `test_keyboard_interrupt_summary_also_dedupes_partial_results` (the fix touched two spots - the normal-completion path and the `KeyboardInterrupt` partial-results path - this covers the second one). 262 tests total, ruff-clean.

## 1.6.7 — Design/readability pass, scoped to source only: no test changes

A source-only code-quality review (design and readability, not new bug-hunting - F1-F47 already covered correctness/security) with one deliberate constraint set upfront: nothing in `tests/` would be touched, so every finding had to be behavior-preserving enough to land without a single test edit. Four changes made it through that filter; 259 tests, unchanged file-for-file, still pass.

- **`enum.py`'s two tiered warnings (skipped targets, blanket-rejecting targets) had independently hand-written copies of the same "1 item / 2-5 items / 6+ items" branching.** Not just a DRY nitpick: `1.5.0`'s own changelog entry (F37) documents this exact duplication already drifting out of sync once, when the blanket-rejection copy shipped without the `>5` cap the skipped-targets copy already had. Extracted a shared `_log_tiered(items, log_single, log_few, log_many)` helper that owns the three-way branching once; each caller still supplies its own message wording/logger level via callbacks, since the two warnings differ in both (`logging.WARNING` vs. `BLANKET_WARN`, a bare target string vs. a `(target, code, user)` tuple) - only the branching itself, the part that actually drifted before, is now shared and can't drift again.
- **`logging_config.py` had three near-identical one-line methods** (`_found`, `_found_unconfirmed`, `_blanket_warn` - each just `if self.isEnabledFor(LEVEL): self._log(LEVEL, message, args, **kwargs)` for a different `LEVEL`). Replaced with a single `_make_log_method(level)` factory, called once per level.
- **`nes.py`'s `_looks_like_file()` was a nested closure inside `run()`**, unlike its four sibling helpers (`_scan_one`, `_resolve_user_pairs`, `_warn_if_experimental_user_lists`, `_resolve_target_networks`), all of which are module-level. It closes over nothing from `run()`'s scope (only takes `path`), so hoisting it to match its siblings was a pure, safe move.
- **`das.py`'s flood loop called `sip_packet.sip_packet(...)` with 8 positional arguments**, while the otherwise-identical constructor calls in `nes.py::_scan_one()` and `enum.py::_check_one()` use keyword arguments. Given the constructor's actual parameter order (`method, server_ip, server_port, client_ip, from_user, to_user, user_agent, sp_user, protocol, ...`), an 8-long positional call is one future signature reorder away from silently swapping `from_user`/`to_user`/`user_agent`/`sp_user` with no error. Switched to keywords, matching the other two call sites.

Not fixed, and explicitly out of scope for this pass: a real camelCase/snake_case naming inconsistency runs through the codebase (`net_utils.py`'s `randomIPAddress`/`writeFile`/`printInital`/`printResult`/`defineTargetType` vs. its own `read_lines`/`check_ip_address`; `das.py`'s per-iteration `toUser`/`fromUser` vs. its own `to_user_choices`), most visibly in `sip_packet.py`'s single class, `class sip_packet:` itself - a class named in snake_case rather than PascalCase is about as clear a PEP8 violation as Python naming gets. Checked why `ruff check` never caught any of this: `pyproject.toml`'s `[tool.ruff.lint]` has no explicit `select`, so only ruff's default rule set (`E4/E7/E9/F`) runs - `N` (pep8-naming) was never enabled. Left alone here because every one of these names appears verbatim in `tests/` as an import or a call target; renaming any of them would mean editing test files, which this pass's own rule ruled out. *(Correction, found during a later documentation review pass: `pyproject.toml` has always had an explicit `select = ["E", "F", "B", "UP", "C4", "SIM"]`, not no `select` at all - `N` just isn't in that list. The conclusion above is unaffected; only "no explicit select" was wrong.)*

259 tests (unchanged, no test file touched - `git diff --stat` against this pass shows exactly the 4 source files above), ruff-clean.

## 1.6.6 — Stop shipping committed scan output; `output/` is generated, not checked in

A repo-hygiene pass, prompted by a direct question about whether `output/ip_list.txt` and `output/from.txt` needed to be in the repository at all: `output/from.txt` is harmless (plain extension numbers, `1000`-`5006`), but `output/ip_list.txt` contained what reads as real scan-result data - five `ip;user_agent;type` entries, three of them public IPs, in the exact format SIP-NES writes.

Traced the data's origin before deciding what to do about it: `git log --follow` on the file (through its pre-restructure path) points to `b12903c` ("Some fixes and new developments", author `melihtas`, 2019-06-14) - years before this hardening pass started, and unrelated to any of this work. The same content is still present, unchanged, on the upstream project's actual live `master` branch today (`meliht/Mr.SIP`, checked directly), independent of this PR entirely.

That last fact shaped the fix. Scrubbing this from the *entire* repository history (not just this branch's own commits) was considered and rejected: the data was introduced before this branch's fork point, so removing it fully would mean rewriting commits authored by the project's own maintainer and other historical contributors - not this branch's history to rewrite - and would give every commit in the repository a new SHA, leaving this branch's head with no shared ancestry with upstream `master` at all, breaking the PR's mergeability for no actual gain (the data stays exactly as public as before, just on the branch that already has it upstream). A full-history purge is the right tool for a leaked *credential*, where every copy must be treated as compromised regardless; this is stale, years-old scan output on someone else's already-public branch, not a secret this fork can unilaterally erase from the record.

What's within this branch's own control - and what actually changed: neither file is tracked going forward. `output/*.txt` is now `.gitignore`'d. Confirmed this has no functional cost: `net_utils.writeFile()` already creates `output/` on demand (`os.makedirs(parent, exist_ok=True)`), so SIP-NES's first run recreates `output/ip_list.txt` itself, and the tool's actual default wordlists were never `output/from.txt` in the first place - `src/data/wordlists/{fromUser,toUser,spUser,userAgent}.txt` are the real, already-bundled defaults `--from`/`--to` fall back to, untouched by this change. The only source-level reference was `src/cli.py`'s `ENUM_USAGE` help text and its `docs/usage-guide.md` mirror, both showing `--from=output/from.txt` as the custom-wordlist example - reworded to `--from=<wordlist.txt>`, matching the `<target_IP>`-style placeholder convention already used everywhere else in the same help text, instead of pointing at a file that no longer exists in the repo.

259 tests, ruff-clean - no test changes needed (nothing asserted on the removed files' contents or the old example text).

## 1.6.5 — Full documentation verification pass: every git-tracked doc, claim by claim

Prompted directly by `1.6.4`'s own root cause: an assumption (Issabel 5's `chan_sip` selectability implying F6's classic behavior) had been written into `docs/lab-guide.md` as settled fact without confirming the actual runtime behavior. That wasn't an isolated slip - it was a methodology gap, so this pass re-read every git-tracked doc file (`README.md`, `CHANGELOG.md`, `docs/installation.md`, `docs/lab-guide.md`, `docs/mrsip-pro.md`, `docs/usage-guide.md`) claim by claim, researching or verifying anything stated as fact rather than trusting it because it read fluently. Documentation-only, no source/test changes - 259 tests total, unchanged.

### README.md

- **The "130+ projects, 2020" competition claim was wrong, not just imprecise** - already flagged as a discrepancy back in `1.5.1` ("Noted but not changed... left for the tool's author to confirm") but never actually resolved. Fetched the primary source directly this time: the organizer's own LinkedIn results post states 55 applications, 17 finalists, and names Ismail Melih Tas as the gold-medal winner. Corrected the number and the link (the original URL redirected to a region-specific `tr.linkedin.com` post, updated to the resolved one).
- **The "5 novel attack vectors and 3 defense models" claim didn't match the list under it** - only 5 items are listed, not a separable 5+3=8. Reworded to describe the list as what it demonstrably is, without the unverifiable 5-and-3 split.
- **Cisco Press and graduate-thesis citations have no findable primary source** - searched independently, including the claim's own restatement on Mr.SIP Pro's official site (`mrsip.pro/about`), which also gives no book title, thesis name, or institution. Not deleted (no evidence it's false, and it's the tool author's own reported claim about their own work) but explicitly noted as unverified rather than presented with the same confidence as the four academic papers below it, which were each individually confirmed against their publisher (IEEE Xplore, MDPI, ScienceDirect) with matching titles/authors/venues.
- **The TSE (Turkish Standards Institute) collaboration description was reworded to match the author's own more specific phrasing** on `mrsip.pro/about` ("authored the national VoIP security strategy in collaboration with TSE, 2015") rather than this repo's own looser paraphrase.
- **The two Netaş Innovation Challenge placements (2011, 2012) could not be matched to any public primary source** - plausible for an old, likely-internal corporate competition, searched for directly but left un-asserted-as-verified rather than either removed or confirmed.
- **Verified via direct browser navigation** (not just search-engine snippets, which don't reliably index blackhat.com): Black Hat Asia 2023, Black Hat Europe 2022, and the archived Black Hat MEA 2022 speaker page all resolve to real, matching Mr.SIP entries - the "Global Stage Recognition" list's newer additions hold up, not just the ones already checked in the `1.5.1` documentation audit.

### CHANGELOG.md

- **Two historical entries described conclusions now known to be incomplete or already superseded, with no cross-reference forward.** The original `1.2.0` "Known limitation left open" entry for F6 said the heuristic "only breaks against PJSIP" - true as far as it went at the time, incomplete per `1.6.4`/F45. Added a correction note pointing to F45, left the original wording in place as a record of what was understood then. Similarly, `1.3.0`'s "Known observation, not fixed" about SIP-ENUM's liveness pre-check being unthreaded was actually resolved in `1.5.0` (which threaded it through `run_worker_pool()`) - never cross-referenced, so a reader landing on the `1.3.0` section alone would get a stale impression. Added the forward pointer.

### docs/lab-guide.md

- **Trixbox CE 2.8.0.4 was mislabeled "Asterisk 1.4-era" in two places** (the VM disk image list and the "Generalizing" section) - checked against Trixbox's own documented release history: 2.8.0.1 is documented as bundling Asterisk 1.6.0.9, and 2.8.0.4 is a later point release in the same 2.8 branch, so "1.6.x-era" is the defensible figure, not 1.4. Doesn't change `1.6.4`'s core conclusion (both predate Asterisk 1.8's `alwaysauthreject` default change), but the specific version number was wrong and is exactly the kind of detail this pass exists to catch. Also corrected the same mislabeling in `docs/usage-guide.md`.
- **The Docker lab's own example `pjsip.conf` had a comment directly contradicted by this project's own, already-documented F40 finding.** It said extension `1099` (intentionally absent) should make SIP-ENUM "report 404 = no extension" - but F40's live verification (already in this same file, `1.6.0`'s section) found `chan_pjsip`'s blanket-rejection makes `1099` return the identical `401` as the real extensions, not a distinguishing `404`. Corrected the comment and the accompanying design note to describe what actually happens (confirmed/unconfirmed grading, not a status-code split, is what demonstrates the negative case here) - a stale comment inside the lab's own reference config is exactly the kind of drift a careful re-read catches and a fluent-sounding first draft doesn't.
- **Verified, not just assumed**: `andrius/asterisk:latest`'s multi-arch claim (`docker manifest inspect` - confirmed real `arm64` and `amd64` platform entries), the OSDN April 2025 shutdown date, and that the Trixbox CE / Issabel 4 SourceForge download links still resolve (`curl`, HTTP 200 through the mirror network) - all previously-made claims, none blindly re-trusted.

### docs/mrsip-pro.md

- **The "10 modules, 3 categories" table doesn't match the live site's own current marketing framing** ("20+ Attack Modules" in its hero section, a 5-category feature-tab layout - Discovery/Vulnerability/Attack/Media/Automation - rather than 3). Checked directly against the live site rather than assumed accurate from the last pass; not fully reconcilable from outside (the site's tabbed UI doesn't expose a complete list without clicking through each tab, so whether "20+" reflects real growth since this table was last verified, or counts something more granular than "modules," can't be determined externally). Left the detailed table as-is (more specific and useful than the marketing figure) but added a note flagging the discrepancy instead of presenting either number as settled.

### docs/usage-guide.md

- Fixed the same Trixbox CE Asterisk-version mislabeling as `lab-guide.md` above.
- **Verified every `.message` template claim in the `--mt` reference table against the actual template files**, not memory: `subscribe.message`'s hardcoded `User-Agent: Nortel PCC 6.0.155`, `invite.message`'s SDP codec list (Opus/Speex/iLBC/G.726 plus telephone-event DTMF variants at four sample rates) and its `Content-Length: 585` (matching the `1.5.0` fix), `sp-invite.message`'s `Remote-Party-ID` header, `register.message`'s `[[expire_duration]]` placeholder (matching the `1.5.0` fix, not a regression back to the hardcoded `3600`) - all confirmed accurate, none silently drifted.

### docs/installation.md

- Reviewed in full; no unverified claims found - it's general Python/venv/sudo guidance and repo-specific behavior already confirmed live earlier in this project's history, not claims about external, changeable resources.

## 1.6.4 — F6 was framed on the wrong axis: `alwaysauthreject`, not `chan_sip` vs. `chan_pjsip`

A live, unplanned discovery while using the VM lab guide (1.6.3): a fresh Issabel 5 install, with `chan_sip` deliberately selected specifically to reproduce F6's classic-`chan_sip` scenario, still blanket-rejected every SIP-ENUM probe - including a deliberately-nonexistent extension - exactly like the Docker lab's `chan_pjsip` target does. Root-caused by reading Asterisk's own source directly, across multiple version branches, rather than assumed: `chan_sip`'s `alwaysauthreject` setting - which makes every rejected request look identical whether the user exists or not - has been Asterisk's own **compiled-in default since version 1.8 (2011)**, independent of channel driver entirely. `global_alwaysauthreject = 0` in 1.4/1.6.2 (matching Trixbox CE, F6's original clean test case) versus `#define DEFAULT_ALWAYSAUTHREJECT TRUE` in every version 1.8 through 18 checked. This lines up with the same era as Asterisk's own SIP-username-enumeration security advisories ([AST-2013-003](http://downloads.asterisk.org/pub/security/AST-2013-003.html), CVE-2011-2666) - `alwaysauthreject` is Asterisk's own hardening against exactly the vulnerability class SIP-ENUM tests for. Confirmed live against the Issabel 5 box: `sip show settings` reported it enabled with no explicit config line anywhere - purely the compiled default. 1 new regression test, 259 tests total (an assertion added to an existing test, not a new behavior - `_classify_confidence()`'s grading logic was already correct and mechanism-agnostic).

### Documentation corrections

- **`docs/lab-guide.md`**: the Issabel 4/5 entries added in 1.6.3 claimed picking `chan_sip` was sufficient to reproduce F6's classic scenario - it isn't, on either of them, since both bundle Asterisk versions well past 1.8. Corrected to explain the real two-part requirement (a build old enough to predate 1.8 entirely, like Trixbox CE, needs nothing extra; anything newer needs `alwaysauthreject=no` set explicitly), added a verification/fix step (`asterisk -rx "sip show settings"`, then `sip_general_custom.conf` + `sip reload` if needed - never hand-edit `sip.conf` directly, the GUI regenerates it) to the extensions walkthrough, corrected the "Generalizing to other Asterisk-based PBXs" section and the Docker-vs-VM comparison table to match. Also documented Issabel 5's real (non-obvious, verified live) navigation path to the Extensions form - a collapsed classic-FreePBX-style menu behind a hamburger icon inside the "PBX Configuration" page, not Issabel's own outer sidebar.
- **`docs/usage-guide.md`**: rewrote the F6 module-behavior entry - it previously named PJSIP as effectively the sole cause of blanket-rejection; now explains both independent mechanisms (`chan_pjsip`'s endpoint-matching architecture, and `chan_sip`'s `alwaysauthreject` default) with the source-verified version history.
- **`enum.py`'s own blanket-rejection warning text** — previously said "e.g. PJSIP" as if that were the whole explanation; a runtime message repeating the same incomplete attribution the docs did would have left operators with the same wrong mental model even without reading any doc. Reworded to name both mechanisms.

## 1.6.3 — `docs/lab-guide.md` corrections found by actually running the VM lab

Documentation-only - no source/test changes, 259 tests total, unchanged. Two claims in `docs/lab-guide.md`'s VM lab section turned out to be wrong once actually exercised end-to-end against a real Issabel 5 install, rather than just re-read for consistency.

- **"A current Issabel 5 install won't reproduce this lab's `chan_sip` scenario" was false.** A real install from the latest non-alpha Issabel 5 ISO (`issabel5-USB-DVD-x86_64-20240430.iso`, verified against the official IssabelPBX SourceForge listing) presents a "Select default SIP channel driver" step with `chan_sip`/`chan_pjsip` as an explicit either/or choice - `chan_pjsip` is only the pre-selected default, not the sole option. The guide previously treated "currently downloaded, currently maintained" as a reliable proxy for "PJSIP-only," which held for FreePBX (`chan_sip` is unloadable on Asterisk 21) but not for Issabel 5. Corrected to document Issabel 5 as a real option alongside Trixbox CE/Issabel 4, with the same "check the actual project listing before linking it" standard already applied to the other two.
- **The documented `hostfwd=tcp::8088-:80` web-GUI port forward doesn't work against Issabel specifically.** Issabel's Apache unconditionally 302-redirects any plain-HTTP request to HTTPS **on the same port number it received the request on** (confirmed directly: `curl -D - http://127.0.0.1:8088/` → `Location: https://127.0.0.1:8088/`) - it has no way to know host port 8088 is itself just a forward, so it reflects back whatever host:port the browser actually used. With the host port still mapped to guest `80` (plain HTTP only), the browser's follow-up HTTPS request to that same port hits a non-TLS listener and fails outright - not a normal "connection refused," a TLS handshake/protocol error, which doesn't obviously read as a port-forwarding problem. Documented the fix (forward straight to guest `443` instead, browse `https://127.0.0.1:8088/`, accept the one-time self-signed-cert warning) both in the port-forwarding flag table and as a dedicated troubleshooting note in the "Web GUI: adding extensions" walkthrough, specifically flagged as an Issabel behavior (Trixbox's older admin panel doesn't force this).

Both corrections were found live, mid-install, while actually working through the guide rather than re-reading it - the exact gap a documentation-only re-read can't catch, and the reason this project keeps re-verifying its own instructions against real infrastructure instead of treating "it reads correctly" as sufficient.

## 1.6.2 — Dead Pro-version link fix, plus a Mr.SIP Pro documentation pass

A focused pass over `docs/mrsip-pro.md` and its cross-references, prompted by comparing it directly against the live `mrsip.pro` site. Found one real, previously-missed bug and made the doc itself read like it's describing a paid product rather than an internal feature-parity table. 1 new regression test, 259 tests total.

### Correctness fix

- **`cli.py`'s "no module specified" message (shown to every operator who runs `python3 mr.sip.py` with no `--nes`/`--enum`/`--das`) still pointed at `https://mrsip.gitlab.io/`, which redirects to a GitLab Pages auth wall with no reachable content.** `CHANGELOG.md`'s own `1.5.1` entry claims this exact dead link was fixed ("`mrsip.gitlab.io` (referenced twice) ... both fixed ... to `mrsip.pro`") - that pass caught the two doc references (`README.md`, `docs/mrsip-pro.md`) but missed this one, which lives in actual runtime output, not a doc file - so the fix was real but incomplete, and the most-visible instance (a brand-new operator's very first encounter with the Pro upsell) stayed broken. Fixed to `https://www.mrsip.pro/`; added a regression test (`test_no_module_specified_points_to_the_live_pro_site`) asserting the real domain appears and the dead one doesn't, so this specific class of "doc says fixed, code says otherwise" gap can't silently recur here again.

### Documentation

- **`docs/mrsip-pro.md` read like an internal feature-parity spreadsheet, not documentation for a paid product** - accurate, but all table and no framing, with zero visual identity. Rewrote the opening and added a "Why teams upgrade" section explaining *why* the scope split exists (grounded in this project's own already-verified facts - the Black Hat/DEF CON/Offzone presentation history in `README.md`, not invented copy), instead of only listing *what* differs. The technical comparison/module tables themselves were left untouched - they were already accurate, this was a tone and structure pass, not a fact-finding one.
- **Added a real "Explore further" link table** (`Features`, `Pricing`, `Docs`, `Demo`, `Use Cases`, `Research`, `About`, `Contact`) pointing at the live site's actual navigation - verified directly against `mrsip.pro`'s own nav links rather than guessed, since this repo's own rule is to never fabricate URLs. Previously only a single bare `mrsip.pro` link existed at the very bottom of the page. `README.md`'s own Mr.SIP Pro section got the same two highest-value additions (`Pricing`, `Demo`).
- Deliberately did **not** copy the live site's marketing copy (tagline, testimonials, pricing figures) into this repo - reproducing another site's copyrighted text isn't appropriate here, and hardcoded prices would just go stale. Where useful, the doc links out to the real, current page instead of mirroring it.

## 1.6.1 — Template cross-substitution fix, plus a code-quality/test-coverage pass

Issues found during a full-repo code-quality review (not tied to any specific module's feature work): a genuine templating correctness bug in `sip_packet.fill_packet_data()`, a missing test for an existing, already-correct upfront validation check in `cli.py`, and two cosmetic/structural cleanups (no behavior change). 4 new regression tests, 258 tests total.

### Correctness fix

- **`sip_packet.fill_packet_data()` could re-substitute an already-substituted value if it happened to look like a different placeholder.** Each of the 12 known `[[placeholder]]` tokens was replaced via a separate, sequential `text.replace(key, value)` call chained over the same accumulating string - so if an *earlier*-processed field's value (e.g. `from_user`) happened to contain the literal text of a *later*-processed placeholder (e.g. the literal string `"[[call_id]]"`), that literal text got overwritten a second time by the later placeholder's real value once its turn came. Verified empirically: constructing a packet with `from_user="[[call_id]]"` produced a `From:` header containing the packet's actual random Call-ID value instead of the literal string `[[call_id]]` a wordlist might genuinely contain. Severity is low in practice (CRLF/header injection was already independently closed off, and every substituted value is itself already-safe content, not attacker-arbitrary data) - but it's a real, previously-undocumented bug in the templating engine, not a hypothetical. Fixed by switching from N sequential `.replace()` calls to a single `re.sub(r"\[\[(\w+)\]\]", callback, text)` pass over the *original* template text - a substituted value's contents are never rescanned for further placeholders, so this whole class of cross-substitution is closed structurally, not just for the one reproduced case. An unrecognized `[[token]]` (not one of the 12 known placeholders) is still left untouched, matching the old behavior.

### Test coverage

- **`cli.py`'s upfront root-privilege check for SIP-DAS's default (Scapy raw packet / spoofing) mode had no test at all.** The check itself was already correct (added to fail fast with a clean error instead of letting a non-root run crash deep inside Scapy's own raw-socket permission check - confirmed live while writing the missing test: temporarily disabling the check surfaced a raw, uncaught `scapy.error.Scapy_Exception`, exactly the failure mode the check exists to prevent), it just had zero regression coverage. Added 3 tests: DAS without `-l` as non-root is a clean exit with a "run with sudo or use -l" message (not a traceback); DAS with `-l` as non-root skips the check entirely (spoofing-free socket mode needs no root); DAS without `-l` as root also skips it. No behavior change.

### Cosmetic / structural cleanup (no behavior change)

- **`sip_packet`'s class docstring was just a bare list of the 12 `[[placeholder]]` token names, with no description of what the class actually does.** Added a real summary (builds and sends one SIP message from a template; `fill_packet_data()`/`generate_packet()`'s roles; one instance = one message) above the existing placeholder list, which is kept as reference.
- **`nes.py`'s single-target synchronous probe path (in `run()`) duplicated the bulk path's `_scan_one()` almost verbatim** - same packet construction, send, and `printResult()` call, just inlined instead of shared. `run()` now calls `_scan_one()` directly for the single-target case instead of the thread pool, removing the duplication; existing tests for both paths (`TestNesRunSingleProbe`) pass unchanged, confirming the refactor is behavior-preserving.

## 1.6.0 — SIP-ENUM confirmed/unconfirmed grading for the F6 blanket-rejection heuristic

SIP-ENUM's F6 limitation (401/403 = "extension exists" is unreliable against a modern PJSIP target that blanket-rejects every unmatched request the same way) has been a documented, open issue since `1.0`. Since `1.3.0`, SIP-ENUM has sent a pre-flight probe with a random nonexistent user and warned the operator once if that probe also got a 401/403 back - but the warning was the whole mitigation: every real finding afterward was still reported with the same flat confidence, whether it came from a genuinely distinguishing chan_sip-style response or a target that rejects literally everything. This release keeps that same pre-flight probe's response code around as a per-target baseline, grades every individual finding against it, and - after live testing surfaced that a textually-"unconfirmed" result still looked identical to a real one at a glance - gives confirmed vs. unconfirmed results genuinely distinct console coloring. No new capability was added to see through a genuinely blanket-rejecting server - that's not possible from the client side, the target isn't leaking the information needed - this is a confidence/reporting improvement: SIP-ENUM now tells the operator honestly which findings it can and can't stand behind, instead of reporting all of them the same way. 24 new regression tests, 254 tests total.

### New capability

- **SIP-ENUM findings are now graded confirmed/unconfirmed against the F6 blanket-rejection baseline.** `_resolve_live_targets()`'s pre-flight probe result (previously discarded after deciding whether to print the F6 warning) is now kept as a `baseline_map` and threaded into the real enumeration pass. A 401/403 finding that exactly matches its target's own baseline code is reported as unconfirmed, everything else - including any target scanned with `--skip-live-check`, where no baseline exists - stays confirmed, identical to the pre-1.6.0 behavior. `code == 200` ("no auth required") is always confirmed regardless of baseline: a server letting *any* random user in without auth is itself a real finding, not noise to suppress.
- **Confirmed and unconfirmed findings are now visually distinct, not just textually.** Two new custom log levels (`logging_config.py`, same pattern `FOUND` itself was originally added with): `FOUND_UNCONFIRMED` renders as a yellow `[ FOUND ]` (vs. a confirmed result's green one); `BLANKET_WARN` renders as a red `[ WARN ]`, reserved for SIP-ENUM's F6 blanket-rejection warning specifically so it doesn't blend into routine warning noise elsewhere in a run. Both get distinct names in the on-disk log too, so they stay greppable there, not just visually distinct on a color terminal. The summary panel gained a matching count split ("N found (C confirmed, U unconfirmed - blanket-reject suspected)"; a run where every finding is confirmed keeps the exact old wording unchanged). `_check_one()`'s per-probe DEBUG line (`-v`) now also shows the baseline code a result was graded against.
- **SIP-ENUM's FOUND log lines now surface response header data that was already being parsed but silently discarded.** The `WWW-Authenticate` realm is appended to 401/403 findings (e.g. `(realm: asterisk)`); the `Server`/`User-Agent` header is appended to 200/no-auth-required findings. `sip_packet.getResponse()` has parsed every response header into a dict since early on; `_check_one()` previously only ever read the status code out of it.
- **Verified live** against a Docker `andrius/asterisk` PJSIP lab (`docs/lab-guide.md`'s Docker lab, three real digest-auth extensions `1000`-`1002`, `1099` intentionally absent): SIP-ENUM against a 5-entry list (`1000, 1001, 1002, 1099, 9999` - 3 real, 2 fake) got `401` with `realm="asterisk"` for all five, indistinguishable from each other - reproducing F6's blanket-rejection exactly. The new grading reported **0 confirmed, 5 unconfirmed**, correctly flagging the entire result set as untrustworthy from this signal alone, instead of the old behavior's flat "5 SIP extension(s) found." A pseudo-tty capture (`script`, since a piped capture auto-disables color) confirmed the actual byte sequences: `\033[33m[ FOUND ]\033[0m` for the unconfirmed lines, `\033[31m[ WARN  ]\033[0m` for the blanket-rejection warning.

No CLI flags added or changed, no change to SIP-NES, SIP-DAS, the thread pool, or the NES→ENUM `ip_list.txt` data flow - contained entirely to `enum.py`'s internal enumeration logic plus the two new log levels in `logging_config.py` (deliberately scoped: every other warning in the tool is untouched).

## 1.5.1 — Edge-case and error-handling hardening pass

A focused review pass over edge cases and error handling across all three modules - no new capability, no flag changes, no interface change (one existing flag, `--mtu`, gained a new upfront validation error in one specific combination). 6 new regression tests, 230 tests total. Also includes a documentation-only follow-up pass (below) - no code, no test count change from that part.

### Correctness fixes

- **SIP-DAS crashed with a raw `IndexError` when a `--to`/`--from`/`--su`/`--ua` wordlist resolved to zero usable lines** (an empty file, or one entirely filtered out by the `isalnum` check `--to`/`--from`/`--su` apply) - `random.choice([])` on the flood loop's very first iteration raised immediately, with no upfront validation. Fixed with an empty-list check for all four wordlists, raised through the same `value_errors`/`check_value_errors()` pattern every other DAS input-validation error already uses.
- **Ctrl+C during a SIP-NES/SIP-ENUM run exited 0 instead of the standard 130 for SIGINT.** `1.4.0`'s fix for "Ctrl+C threw away the summary panel" made `run_worker_pool()` swallow `KeyboardInterrupt` and return normally with the partial results - which preserved the summary, but as a side effect meant `cli.main()`'s own `sys.exit(130)` handler was never reached from this path, so an automation script checking the exit code couldn't tell an interrupted run from a completed one. `run_worker_pool()` now still collects the partial results and lets the caller log its summary, but re-raises `KeyboardInterrupt` afterward (with the results attached as `.results`) instead of swallowing it, so the interrupt still reaches `cli.main()`'s existing handler and exits 130 as everywhere else in the tool. SIP-DAS's flood loop was unaffected - it never wrapped `KeyboardInterrupt` in the first place.
- **`net_utils.expand_target_network()` had no guard against expanding a huge range/CIDR into memory.** A mistyped mask (e.g. `/8` instead of `/24`) had nothing stopping it from building a full Python list of every address at once. Added a warning (not a hard block, consistent with this tool having no dry-run gate) whenever the requested range/subnet exceeds 65,536 addresses.
- **SIP-DAS silently accepted `--mtu` together with `-l` (socket-library mode), where fragmentation has never applied** (it's a Scapy-only code path). Now a clean upfront error, the same style as the existing `-r`/`-s`/`-m` + `-l` spoofing warning for the same underlying reason.
- **SIP-DAS's socket-library flood mode (`-l`) opened and closed a brand-new UDP socket per packet**, risking local ephemeral port exhaustion (`EADDRINUSE`) on a long/fast flood - the `-l`-mode counterpart to `1.5.0`'s F26 fix, which gave Scapy/raw mode a single persistent socket for the same reason. `sip_packet` gained a `client_socket=` constructor parameter mirroring the existing `scapy_socket=` one; `das.py` now opens one persistent UDP socket before the flood loop in `-l` mode and reuses it for every packet.
- **`sip_packet.getResponse()` failed to parse a single-line SIP response with no headers at all**, or with no reason phrase (`SIP/2.0 401` instead of `SIP/2.0 401 Unauthorized`) - some non-compliant SIP devices reply this way. Both are now parsed correctly instead of returning an empty result and losing the status code.
- **Printing an error containing a character the terminal's encoding can't represent could itself crash, specifically on `stderr`.** `1.3.1` already reconfigured `sys.stdout` with `errors="backslashreplace"` for this; `sys.stderr` (where an uncaught traceback actually goes) was missed. Now both streams are reconfigured the same way.

### Documentation

- **`docs/lab-guide.md`'s VM lab section previously pointed at a Trixbox CE `.ova` and, in an earlier draft of this pass, an Issabel 4 `.ova` that turned out not to exist** - checking each project's actual SourceForge file listing directly (rather than trusting a summary) showed both are ISO-only. Rewritten to link real, individually-verified ISO downloads for both (Trixbox CE 2.8.0.4; Issabel 4's newest non-beta/non-nightly build), drop a dead OSDN mirror link (OSDN shut down entirely in April 2025), and remove every remaining `.ova`-handling instruction from the guide instead of leaving a generic branch for a format neither linked download actually uses.
- **Added an acceleration-flag decision table and a set of anticipated first-run failure modes to the VM lab section**: which of `-accel tcg`/`hvf`/`kvm` applies on Apple Silicon vs. Intel Mac vs. Linux x86_64 vs. Linux ARM64 (neither Mac/Linux ARM host has a faster option for these x86_64 images), plus concrete guidance for a stuck/slow installer, a QEMU window not receiving keyboard focus, and (root-caused during actual use) an installer hang tied to picking a timezone/locale that doesn't match the operator's real location.
- **Same depth pass applied to the Docker lab section**: confirmed `andrius/asterisk:latest` is genuinely multi-arch (no emulation needed on Apple Silicon/ARM Linux, unlike the VM lab), and added the container's real `HEALTHCHECK` `start-period` (expect `starting`, not `healthy`, for the first ~30s), a port-already-allocated scenario, a container-name-conflict scenario (`docker run` vs. `docker start`), and a config-edited-but-not-reloaded scenario (the Docker-lab equivalent of the VM lab's "Continue With Reload" step).
- **`docs/usage-guide.md`'s Global Parameters table was missing `--skip-live-check` entirely** despite the flag existing in `src/cli.py` and being named in this file's own `1.5.0` entry above - added, along with a fuller explanation of which module(s) it actually affects (not SIP-NES, which has no liveness pre-check to skip).
- **The `register` message-type row described `Expires: 3600` as a fixed value**, inconsistent with the adjacent `subscribe` row's correct `[[expire_duration]]` phrasing, and stale relative to the `1.5.0` fix that made `register.message` use the same placeholder. Reworded to match.
- **Added a short explanation of the `chan_sip`/`chan_pjsip` distinction** (Asterisk's two SIP channel drivers; `chan_sip` deprecated in Asterisk 17, removed in 21 - confirmed against Asterisk's own "New in 21" documentation) directly in SIP-ENUM's blanket-rejection note, since the rest of that note assumed the reader already knew what "classic `chan_sip` behavior" meant.
- **Two dead/degraded external links fixed in `README.md`/`docs/mrsip-pro.md`**: the Offzone Moscow badge pointed at `offzone.moscow/report/...` (confirmed 404; a working `2019.`-prefixed variant already existed elsewhere in the same file), and `mrsip.gitlab.io` (referenced twice) now redirects to a GitLab Pages internal auth wall with no reachable content - both fixed to link `2019.offzone.moscow/...` and `mrsip.pro` respectively, each individually re-verified after the fix.
- **Noted but not changed**: `README.md`'s "130+ projects" claim for the 2020 competition placement doesn't match the actual linked LinkedIn post's own numbers (55 submissions, 17 finalists) - left for the tool's author to confirm rather than guessed at. **Resolved in `1.6.5`** (see that section): fetched the primary source directly and corrected the number.

## 1.5.0 — Liveness pre-checks, SIP-ENUM range/CIDR support, and a broad hardening pass

SIP-ENUM and SIP-DAS both used to burn a full probe's worth of timeout against a target that never answers at all - fixed with a shared liveness pre-check. Combined with a follow-up fix to `1.2.0`'s own F20 mitigation (a mixed-default `--from`/`--to` case it missed), and a wide correctness/reliability pass covering everything from a raw traceback on a bad `--if` value to an RFC 3261 `Content-Length` violation in `invite.message` that a live send can't surface on its own. 83 new regression tests, 224 tests total.

### New capability

- **SIP-ENUM/SIP-DAS liveness pre-check** (`net_utils.probe_liveness()`): before enumerating or flooding, a short (2s), non-blocking probe checks whether the target responds at all. SIP-ENUM skips and warns about any target that doesn't respond, and raises a clean error if *none* respond ("nothing to enumerate"); SIP-DAS only warns, never blocks - it deliberately has no dry-run gate (see the "Commands" section of this project's own design notes: an operator may legitimately want to flood a target that stays silent to casual probes). Both are skippable with the new `--skip-live-check` flag. Threaded through `run_worker_pool()` for SIP-ENUM (was sequential, one probe at a time, when first added), so it doesn't itself become the slow part of a large scan.
- **New `--rt`/`--response-timeout` flag** (SIP-NES/SIP-ENUM): tunes the per-probe response timeout, previously hardcoded to 5s with no way to shorten it. A range scan dominated by non-responsive hosts (the normal case for a network scanner) was capped at `thread_count / timeout` regardless of `--tc`, which looked like SIP-NES wasn't really multi-threaded - it was, the fixed 5s timeout was just the dominant cost. Confirmed live: `--rt 0.5` against a set of guaranteed-dead hosts gave a ~10x throughput increase, matching the timeout ratio exactly. Explicitly setting `--rt` also widens the liveness pre-check's own timeout to match (it otherwise keeps its fast 2s default) - without this, a genuinely slow-but-live target would still be wrongly filtered out as unreachable regardless of how patient `--rt` told the real probes to be.
- **SIP-ENUM now accepts a single IP, a range, or a CIDR for `--tn`** (previously single-IP only), sharing the same range/CIDR expansion SIP-NES already used (`net_utils.expand_target_network()`). Each resolved target gets its own liveness pre-check before enumeration starts.
- **New `-y`/`--yes` flag**: auto-answers the "N packets will be generated, continue?" confirmation for scripted/non-interactive use. A non-interactive shell (no controlling TTY) already refuses to proceed without it rather than silently defaulting either way.
- **SIP-DAS now rejects a range/CIDR `--tn` with a clean error** instead of silently accepting it - DAS floods one fixed target by design (`IP(dst=args.target_network).src` and the whole flood loop assume a single address), so a range/CIDR value (now valid input for SIP-NES/SIP-ENUM) needed an explicit guard here.
- **SIP-ENUM's F6 blanket-rejection warning (`Target %s returned %d for nonexistent user...`) now aggregates across a run instead of repeating once per affected target.** A single affected target still gets its own specific line; 2-5 name every target in one line; 6+ collapse to a count-only summary. Confirmed live: 10 blanket-rejecting targets used to produce 10 nearly-identical WARN lines back to back - on a real `/24`-derived host list sharing the same server software, this read as log spam rather than one clear signal.

### Correctness fixes

- **F20 follow-up - the original `1.2.0` fix only collapsed SIP-NES's `--from`/`--to` cross-product to a single probe when *both* sides were left at their bundled defaults.** Leaving just one side on the default while explicitly overriding the other (e.g. a 9-entry `--from` with `--to` left at default) still cross-producted against the *other* side's untouched 9,000-line default - confirmed live against a `/24`: `9 × 9,000 × 254 = 20,574,000` queued packages, a ~77-day `tqdm` ETA, matching the exact numbers from a real bug report against this tool. Root cause was one `and` that should have been two independent per-side checks. Fixed by collapsing each side of `--from`/`--to` to its first entry independently, based on whether *that specific side* is still at its own default. Verified live: the same `/24` + explicit-`--from` scenario now reports 2,286 packages instead of 20,574,000.
- **SIP-DAS's raw-Scapy flood mode (the default, without `-l`) opened and closed a brand-new raw socket for every single packet.** `scapy.sendrecv.send()` with no `socket=` kwarg does exactly this on every call - well-documented Scapy overhead, and a direct hit against the one code path whose entire purpose is sending as fast as possible. Now opens one persistent socket for the whole run (the outgoing interface is resolved once, the same way the existing per-run `default_client_ip` invariant already relied on) and reuses it for every packet, closed in the same `finally` block that already restores promiscuous mode. `-l` (socket-library/UDP) mode has a smaller version of the same pattern, but UDP socket setup is far cheaper than raw-socket construction, so it was left alone.
- **SIP-NES's `ip_list.txt` writer reread and rewrote the entire output file on every single newly-found host** (O(n) work per discovery, O(n²) total across a scan), under the same lock that serializes every worker thread's result write. Replaced with an in-memory set of already-written targets (loaded once per output path, including any pre-existing content from a prior run against the same file) - now an O(1) membership check with no file rewrite at all for the common case.
- **A bad `--if` value crashed with a full raw Python traceback** through Scapy's own `conf.iface` property setter, instead of the clean error every other bad-input case in this tool gets - the assignment ran *before* `cli.main()`'s own `try:` block even existed. A typo'd interface name is one of the most common mistakes to make with this flag. Now wrapped and raised as the same `InvalidInterfaceError` this tool already used for the sibling "interface exists but has no IPv4" case.
- **A directory given instead of a file for `--from`/`--to`/`--su`/`--ua`/`--il`, or for `-i`/`--ip-save-list`, crashed with a raw `IsADirectoryError`/`PermissionError` traceback**, leaking the local filesystem path. Now caught and re-raised as a clean, specific error message; a genuinely missing file still gets its own distinct "File not found: X" message, unchanged.
- **An unsupported/misspelled `--mt` value produced a technically-caught-but-still-raw message** (`[Errno 2] No such file or directory: '/full/local/install/path/.../garbage.message'`), leaking the local install path with no hint of a valid value. `--mt` is intentionally free-text (custom `.message` templates are a supported feature), so this can't be an argparse `choices=` validator - now lists the real bundled method names instead.
- **`net_utils.promisc()`'s exit-code check could never actually fire** - `os.system()` on POSIX returns a packed wait-status, not the raw exit code, so a real permission failure (exit 1) came back as `256`, and `if ret == 1:` never matched. Switched to `subprocess.run([...]).returncode`, the real exit code.
- **`-c`/`--mtu` had no upfront argparse validation**, unlike every other numeric flag - a bad value only surfaced deep inside a flood loop as a wall of independently-caught per-packet errors, or (for a negative `-c`) silently sent zero packets instead of erroring. New validators, same pattern as the existing ones. Separately, `sip_packet.generate_packet()`'s own MTU guard used a truthy check that treated `--mtu 0` the same as "not given" - now an explicit `is not None` check.
- **`invite.message` declared `Content-Length: 607`, but its actual static SDP body (verified by running the real templating code path, not just reading the file) is 585 bytes** - a malformed message per RFC 3261 §20.14, invisible from a live send the same way an earlier fix (an unclosed `<` in `bye`/`cancel`/`sp-invite`'s `From:` header) was, since SIP-DAS's flood mode never waits for a response to notice a target rejecting it. Fixed the hardcoded number; added a static Content-Length-vs-real-body check to the test suite so this class of bug can't silently recur.
- **`register.message` hardcoded `Expires: 3600`** instead of using `[[expire_duration]]` the way `subscribe.message` correctly does - `sip_packet`'s `expire_duration` constructor parameter (not currently exposed via any CLI flag, so no current operator-visible effect) was silently ignored for REGISTER. Fixed for template consistency.
- **`expand_target_network()` (the new shared range/CIDR expansion) caught bare `except Exception`** around IP-address parsing - the same anti-pattern already fixed elsewhere in this tool (a real programming bug would be silently relabeled as a user-input validation error instead of surfacing as itself). Narrowed to the actual exception types malformed input raises; its "no error list given" fallback path also now raises this tool's own clean error type instead of a bare `ValueError` nothing downstream would catch.
- **`nes.py`'s 125-line `run()` had grown into the most branch-heavy function in the codebase with zero dedicated tests.** Split into three focused, independently-tested helpers (default-vs-explicit `--from`/`--to` resolution, the experimental-wordlist warning, and dash-range/CIDR/single-target expansion), behavior preserved exactly (verified against both the new unit tests and a live target). `--from`/`--to` also now get a clean "file not found" error when the value looks like a file path but isn't one, instead of silently falling back to treating it as a literal username.
- **"package"/"packet" terminology was inconsistent in real operator-facing text**, not just docs: the actual confirmation-prompt wording ("There will be N packages generated") and this tool's own `--help` usage examples (`-c <package_count>`) said "package" while `-c`'s own flag description already said "packet count". Renamed consistently to "packet" throughout.

### Documentation

- READMEs split into a dedicated `docs/` guide set (`installation.md`, `lab-guide.md`, `usage-guide.md`, `mrsip-pro.md`) with the root `README.md` reduced to a short landing page + index, so day-to-day usage detail isn't all crammed into one file.
- A line-by-line pass over every doc file and every `src/data/method/*.message` template fixed several stale/inconsistent claims found alongside the `Content-Length`/`Expires` fixes above: a stale test count, a Python-2-era `apt-get install python-scapy` line left over after the venv-based install flow was already adopted, an incomplete `--ua` description (only `invite`/`register` actually use it), a self-contradictory explanation of the `--rt` throughput cap, and an `invite.message` `Contact`-header explanation that had the client/target address backwards.

## 1.4.0 — DAS flood control, and Ctrl+C summaries everywhere

New capability requested directly by the operator using the tool day-to-day: infinite flood mode, rate limiting, and never losing the run summary to an interrupt. `--dry-run` was considered and deliberately not added. 7 new regression tests (`tests/test_das.py`, `tests/test_threadpool.py`), 141 tests total.

### New capability

- **`-c 0` now means flood indefinitely** (SIP-DAS), matching the `hping3`/`nping` convention. Previously `-c 0` was a degenerate no-op: `while i < counter` never entered the loop, so it silently sent zero packets - nobody runs `-c 0` meaning "send nothing" on purpose. `-c` (unspecified) keeps its existing effectively-infinite default (`99999999`); this only gives `0` itself a real, intentional meaning. `tqdm`'s progress bar switches to an unbounded counter (`total=None`) in this mode instead of showing `0/0`.
- **New `--pps`/`--packets-per-second` flag (SIP-DAS)** throttles the flood to at most N packets/sec, via a new `net_utils.positive_float()` argparse validator (rejects 0 and negative values - a 0 rate would make the per-packet sleep interval infinite). Default is unthrottled, unchanged from before. Useful for an operator who deliberately wants a slower, more controlled test run instead of saturating the link/tripping an IDS immediately.

### Correctness fixes

- **Ctrl+C during a SIP-DAS flood used to throw away the final summary panel.** The old `except KeyboardInterrupt: ... raise SystemExit from None` fired *before* the `_summarize()`/`theme.panel()` call that reports packets/sec and sent/failed counts - meaning the single most common way an operator actually stops a flood (it's either infinite or a very large default counter; nobody waits for it to finish) skipped the one thing that instrumentation exists for. The summary is now computed in a `finally` block that runs on every exit path (normal completion or interrupt), and `KeyboardInterrupt` is no longer swallowed - it still propagates to `cli.main()`'s existing handler, which prints "Interrupted by user." and exits 130, unchanged.
- **Same class of bug in `threadpool.run_worker_pool()` (SIP-NES/SIP-ENUM).** Ctrl+C during a bulk scan/enumeration re-raised past `work_queue.join()`, skipping the caller's own "N found" summary panel (`nes.py`/`enum.py`, logged right after the call returns). Now logs the interrupt warning and returns whatever results the worker threads had already completed and queued, instead of re-raising - the caller's existing summary-logging code then runs normally on the partial result set. (Exit code is 0 in this path rather than 130, since the operation is treated as having produced a valid, if early, result - not a crash.)

## 1.3.1 — Property-based fuzz testing pass

A dedicated fuzzing pass using [`hypothesis`](https://hypothesis.readthedocs.io/) (new dev-only dependency, `tests/requirements-dev.txt`) against every function that parses untrusted input: CLI argparse validators, SIP packet templating, SIP response parsing, and wordlist file reading. Pure bug fixes - no new capability, no flag changes, no interface change. 12 new regression tests (`tests/test_fuzz.py`), 134 tests total.

### Correctness fixes

- **`check_ip_address()` crashed on a value with more than one `/`** (e.g. `--tn=1.2.3.4/24/extra`). The CIDR branch did `ip, subnet = value.split("/")`, which raises an unhandled `ValueError: too many values to unpack` for 3+ parts - argparse's own exception handling happened to still turn this into a usable (if generic) error message when reached via `--tn`, but calling the function directly (as the test suite already does) crashed outright. Now validates the split count first and raises the intended `argparse.ArgumentTypeError`.
- **`sip_packet.fill_packet_data()` crashed on a lone Unicode surrogate character.** A CLI argument containing invalid-UTF-8 bytes is decoded via `sys.argv`'s `errors="surrogateescape"`, producing a string with a lone surrogate; the function's final `.encode("utf-8")` raised `UnicodeEncodeError` on it, crashing the whole run with a raw traceback - reproduced live via `--nes --tn=127.0.0.1 --mt=options --from=<invalid-utf8-byte>`. Every substituted value is now round-tripped through `encode("utf-8", errors="ignore").decode("utf-8")` alongside the existing CRLF-stripping, dropping any character that can't be represented before it ever reaches the template.
- **Console and file logging could themselves crash on an unencodable log message** (e.g. the surrogate case above, echoed back in a "File not found" error message) - `TqdmLoggingHandler`'s internal error handling caught it and degraded to a `--- Logging error ---` dump on stderr instead of a full crash, but that's still noise instead of the intended clean error. `setup_logging()` now reconfigures stdout (`sys.stdout.reconfigure(errors="backslashreplace")`) and the file handler (`logging.FileHandler(..., errors="backslashreplace")`) to gracefully escape unencodable characters instead of raising.
- **`net_utils.read_lines()`/`read_ip_list()` crashed with a raw `UnicodeDecodeError`** on a wordlist file that isn't valid UTF-8 (a binary file pointed at by mistake, a wordlist saved in Latin-1/Windows-1252, ...) - every other bad-input case in this codebase gets a clean CLI error, this one didn't. Now catches the decode error and raises `errors.MrSipError` with a clear message, caught centrally in `cli.main()` like everything else.
- **`--dp`/`--destination-port` had no range validation** (unlike `--tc`, which does). A value outside 0-65535 reached `socket.connect()` and raised a raw `OverflowError: port must be 0-65535` deep inside `generate_packet()`, uncaught by the `except (OSError, UnicodeDecodeError)` clause there (`OverflowError` is neither) - reproduced live with `--dp=99999999` and `--dp=-5`. New `net_utils.port_number()` validator, same pattern as `positive_int()`, rejects out-of-range values at parse time with a clean error.

### Verified robust (no fix needed)

- `sip_packet.getResponse()` - the actual network-facing parser (handles whatever bytes a target sends back, including a hostile or compromised one) - held up against ~6,000 fuzzed random and SIP-shaped-garbage inputs with zero unhandled exceptions; malformed input correctly degrades to a logged warning and an empty/partial result dict, which downstream callers already guard against.
- `build_parser().parse_args()` - 3,000+ fuzzed argv combinations (random flags, values, and orderings) never raised anything but the expected `SystemExit`.
- An extremely long (5,000-character) or Unicode username sent live against both lab targets (Docker/Asterisk and QEMU/Trixbox) either succeeded or failed cleanly (`PacketSendError: Message too long` for an oversized UDP datagram) - no crash.

## 1.3.0 — Themed UI, DRY consolidation, and a second correctness-hardening pass

No CLI behavior is removed and no existing flag changes meaning; a script that worked against `1.2.0` still works. This pass focused on three things: a real terminal UI layer (colors driven by log level instead of hardcoded per call site), removing every remaining case of the same logic implemented two different ways across `nes.py`/`enum.py`/`das.py`, and a from-scratch re-audit of the whole `src/` tree that found and fixed a further round of real bugs beyond what `1.2.0`'s pass covered.

### New capability

- **Themed console output.** `src/core/theme.py` is now the single source of every ANSI color/style used anywhere in the tool. Console log lines get an automatic `[ DEBUG ]`/`[ INFO ]`/`[ FOUND ]`/`[ WARN ]`/`[ ERROR ]` tag, colored by level via a new `logging_config.ColorFormatter` — replacing the old convention of hardcoding escape codes and ad-hoc `[!]`/`[+]` string prefixes inline at each call site. A new custom `FOUND` log level (between `INFO` and `WARNING`) replaces the old `[+]` marker for "live host / valid extension found" results. Color auto-disables when `NO_COLOR` is set or stdout isn't a real terminal.
- **Vulnerability highlighting in ENUM.** Highlight secretless extensions (authentication not required) in red (`theme.ERROR`) to immediately alert security testers of severe misconfigurations during enumeration.
- **Refreshed screenshot assets.** Replaced legacy screenshot files with high-quality terminal captures reflecting the local QEMU Trixbox lab setup (`SIP-NES.png`, `SIP-ENUM.png`, `SIP-ENUM2.png`, `SIP-DAS.png`). Updated `README.md` to reference these new paths and document the new highlight output behavior.
- **New block-style startup banner**, colored through `theme.py`'s constants instead of raw escape codes baked into the banner string.
- **Boxed run summaries.** Each module's final result line is now rendered as a bordered panel (`theme.panel()`) instead of a single unstyled log line.
- **`TqdmLoggingHandler`** (`logging_config.py`) routes console logs through `tqdm.write()` instead of writing directly to stdout, so log lines during a scan no longer corrupt the live progress bar's line.
- **SIP-ENUM blanket-rejection pre-check.** Before enumerating, ENUM now sends one probe per target with a random, guaranteed-nonexistent username; if the target responds 401/403 to it, ENUM warns that the server may be blanket-rejecting everything (e.g. modern PJSIP's default behavior — see F6 below), so the operator knows to treat results with suspicion instead of trusting a silent false-positive.

### Architecture / DRY consolidation

Per-module logic that had drifted into 2–4 different implementations of the same operation was consolidated to one:

- `net_utils.read_lines()` / `net_utils.read_ip_list()` replace four divergent wordlist/`ip_list.txt`-reading implementations previously spread across `nes.py`, `enum.py`, and `das.py` (one of which silently skipped the `isalnum` filter the others applied).
- `threadpool.confirm_bulk_run()` replaces near-identical copy-pasted "N packages will be generated, continue?" prompt-building code in `nes.py` and `enum.py`.
- `net_utils._validate_dotted_quad()` replaces three separate copies of octet-range validation inside `check_ip_address()` (two of which were missing the `ValueError` guard the third had).
- `net_utils.printResult()` now calls `decimal_to_octets()` instead of re-implementing the same int→dotted-IP conversion inline.
- `nes.py`'s three copy-pasted target×user cross-product comprehensions were merged into one, computed once after `target_networks` is determined.
- `net_utils.defineTargetType()` now calls `read_lines()` instead of manually re-implementing its split/filter logic (with a subtly different, unintended behavior — see Correctness fixes).
- Dead `net_utils.readFile()` removed (its only caller was consolidated away).
- New `net_utils.positive_int()` argparse validator for `--tc`/`--thread-count`.
- `sip_packet._read_template()` caches `.message` template file reads (`functools.cache`) instead of re-opening the same file from disk on every `generate_packet()` call — the same class of redundant I/O already fixed for wordlists/`servers.txt` in `1.2.0` (F8/F16), just missed for the templates themselves. Matters most in DAS's flood loop, which calls this up to ~1e8 times at the default counter.

### Correctness fixes

- **OS-assigned UDP port binding:** Modified standard socket client port allocation to bind to port `0`, enabling the OS to automatically assign a free ephemeral port, which is then retrieved via `getsockname()` and filled into the template. This completely eliminates `Address already in use` (Errno 48) bind failures during high-frequency DoS simulations.
- **The y/n confirmation prompt's `setraw`/`setcbreak` bug, actually fixed this time.** `1.2.0`'s changelog and the prior commit's message both claimed a switch from `tty.setraw()` to `tty.setcbreak()` to fix doubled/misaligned keypress output — but the code was never actually changed. `src/core/threadpool.py`'s `_read_single_char()` now genuinely calls `tty.setcbreak()`; verified live over a real controlling terminal (`setsid`/`TIOCSCTTY`) that a single `n` keypress renders correctly with no doubling.
- **`net_utils.printResult()` no longer crashes on a malformed or incomplete SIP response.** `sip_packet.getResponse()` can return `{}` (unparseable status line), fall through to an implicit `None` (no headers past the status line), or store `None` for a header line with no `:`. `printResult()` used to index straight into `result["response"]["headers"]` and then `list(value)`, crashing with `KeyError`/`TypeError` on any of these — reachable from a single malformed reply during a live scan. Now guarded at every level.
- **`--tc=0` (or negative) no longer hangs the tool forever.** `run_worker_pool()` starts `range(thread_count)` worker threads; a count below 1 meant no thread ever existed to drain the queue, so the run spun with no error message until manually killed. `--tc` now goes through the new `positive_int` validator.
- **SIP-ENUM's single-username-wordlist off-by-one.** `if len(user_list) <= 1` rejected a wordlist containing exactly one valid entry as if it were empty, blocking the legitimate "enumerate exactly one extension" case. Now `if not user_list`.
- **`logger.found()` no longer depends on import order.** The custom `FOUND` level is registered as an import-time side effect of `logging_config.py`; `net_utils.py` (which calls `logger.found()` in `printResult()`) now imports `logging_config` itself, so this works regardless of whether `cli.py` has already run first.
- **`check_ip_address()`'s range and plain-IP branches now reject a non-numeric octet the same clean way the CIDR branch already did**, via the new shared `_validate_dotted_quad()` helper — previously they had no guard around `int(number)` (argparse's own exception handling happened to still produce a usable, if generic, error message, so this was never a raw crash, just an inconsistent one).
- **`nes.py`'s file-vs-literal-value detection for `--from`/`--to` switched from a fragile `"txt" in value` substring check to `os.path.isfile(value)`.** The old check misclassified a literal username containing "txt" as a file, and a wordlist path without a literal `.txt` in it as a literal value.
- **`net_utils.defineTargetType()`'s server-list matching now strips whitespace before filtering**, via the `read_lines()` consolidation above — the old manual re-implementation could silently drop a legitimate `servers.txt` entry with trailing whitespace or a stray `\r`.
- **DAS: `-m` without `--il` now raises a clean error instead of a raw `TypeError`.** `args.manual_ip_list` defaults to `None`; `net_utils.read_ip_list(None)` used to call `open(None)` directly.
- **DAS: `-s`/`--subnet` without a valid interface netmask now raises a clean error instead of crashing mid-flood.** A `client_netmask` of `None` used to reach `randomIPAddressFromNetwork()` and fail deep inside the send loop.
- **DAS: promiscuous mode is now guaranteed to be turned back off**, via `try/finally`. It previously only ran on the `KeyboardInterrupt` path and after normal completion — any other exception (e.g. an empty `--il` list making `random.choice` raise `IndexError`) left the interface stuck in promiscuous mode.

### Performance fixes

- **`das.py`'s flood loop no longer recomputes a Scapy routing-table lookup (`IP(dst=...).src`) on every iteration.** `args.target_network` is invariant across the loop, and the value was discarded immediately whenever `-r`/`-s`/`-m` overrode it anyway — now computed once, up front.
- **`run_worker_pool()` simplified back down to one completion-tracking mechanism.** An earlier iteration of this pass had it running `queue.join()` alongside a *second*, separate daemon thread polling `unfinished_tasks` every 50ms just to drive the progress bar — more machinery than needed, and the timeout-bounded teardown of that second thread had a small theoretical race with `pbar.close()`. Each worker now updates the (lock-guarded) progress bar itself right after finishing its own item; `queue.join()` alone decides when the function returns.

### Testing

~50 new pytest cases (`test_theme.py`, `test_logging_config.py`, `test_enum.py` are new files; `test_net_utils.py`, `test_threadpool.py`, `test_das.py`, `test_sip_packet.py` gained new cases), including regression tests for the `setcbreak` fix, the `printResult`/`check_ip_address`/`positive_int`/template-caching fixes above, and the blanket-rejection pre-check. 122 tests total; `ruff check src/ tests/ mr.sip.py` clean.

### Known observation, not fixed

SIP-ENUM's new blanket-rejection pre-check runs one synchronous probe per target *before* the bulk-confirmation prompt, unthreaded. For a single `--tn` target this is one extra packet; for a large `output/ip_list.txt` (e.g. a full `/24` from a prior SIP-NES run), it adds up to one sequential round-trip per host before the operator is even asked to confirm the run. Not incorrect, just a potential wait on large host lists - left as-is pending a decision on whether to thread it through `run_worker_pool()` or move it after confirmation. **Resolved in `1.5.0`** (see that section's "New capability" list above): the pre-check was threaded through `run_worker_pool()`, closing this exact gap.

## 1.2.0

Technical summary of the modular refactor and correctness/security hardening pass on top of upstream Mr.SIP `v1.1.0`. Baseline for comparison is commit `bdd98ad` ("Update OffzoneMoscow2019badge.svg") — the last commit before this work started.

No CLI behavior is removed and no existing flag changes meaning. Two new flags were added (`--mtu`, `--version`). Everything else is a bug fix, a hardening change, or an internal restructure; a script that worked before still works, generally with more accurate output.

Current version: `1.2.0` (from upstream's `1.1.0` - a minor bump, since the CLI surface is unchanged and nothing here is a breaking API change for end users).

## Table of contents

- [New CLI capability](#new-cli-capability)
- [Long-term maintainability](#long-term-maintainability)
- [Architecture changes](#architecture-changes)
- [Correctness fixes](#correctness-fixes)
- [Security fixes](#security-fixes)
- [Reliability / concurrency fixes](#reliability--concurrency-fixes)
- [Performance fixes](#performance-fixes)
- [Usability / error-handling fixes](#usability--error-handling-fixes)
- [Known limitation left open](#known-limitation-left-open)
- [Testing](#testing)
- [Baseline vs. current: reproducible comparison](#baseline-vs-current-reproducible-comparison)
- [Upgrade notes](#upgrade-notes)

---

## New CLI capability

| Flag | Module | Description |
|---|---|---|
| `--mtu <bytes>` | SIP-DAS | In Scapy mode, fragments each outgoing packet to the given MTU via Scapy's `fragment()`. Rejects values below 68 bytes (minimum IPv4 MTU) with a clean error instead of producing malformed fragments. |
| `--version` | global | Prints `Mr.SIP <version>` and exits. `__version__` existed in `src/__init__.py` before this but was never actually wired up to anything - dead code. |

Everything else in `--help` is unchanged flag-for-flag from upstream; only the help text and a few defaults (see [Upgrade notes](#upgrade-notes)) were reworded/repathed.

## To-Do.md resolution

Upstream shipped a `To-Do.md` with 6 items. Reviewed each against the public repo's actual scope (3 modules: NES/ENUM/DAS) and removed the file once every in-scope item was addressed:

1. *"SIP-ENUM will be integrated"* - done; it's one of the 3 public modules.
2. *"SIP-ASP will be integrated"* (+ two DDoS sub-scenarios) - **out of scope**. SIP-ASP is a Pro-only module (confirmed against the Mr.SIP Pro technical guide); it was never part of, and isn't being added to, this repo.
3. SIP-DAS features:
   - a) *"Fragmentation and custom MTU value set support"* - done, this is the `--mtu` flag above.
   - b) *"Network level IP spoofing support"* - already present since the 2017 initial commit (`-r`/`-s`/`-m`, Scapy IP-layer spoofing); confirmed via `git log`, not something added here.
   - c) *"Instrumentation"* - added: SIP-DAS's final summary line now reports actual packets/sec (wall-clock elapsed time, not the requested count) and average per-call send time in ms. Send timings are accumulated as a running sum/count rather than a list, so this doesn't add per-packet memory overhead even at the ~1e8 default flood counter (the same lesson as F8). Unit-tested in `tests/test_das.py` (the summary-formatting logic is a pure function, isolated from the network loop specifically so it's testable without a live target); verified live against Docker (`"20 packet sent to 127.0.0.1... 11968.3 packets/sec, avg send time 0.1 ms."`).
4. *"Verbose mode support"* - done; `-v`/`--verbose` now actually toggles DEBUG logging (previously a no-op).
5. *"Input validation should be performed"* - done extensively (F15, the CRLF-injection fix, `check_ip_address`'s bugs, argparse `type=int` on numeric flags, etc.).
6. *"The whole code should be reviewed for all possibilities and exceptions"* - done; this was effectively the scope of the entire F1-F21 pass plus the `ruff`/exception-chaining cleanup.

## Long-term maintainability

Everything in this branch up to this point was verified by hand against a live Docker/Asterisk target and a QEMU lab - thorough, but not repeatable and not something a future contributor's PR gets checked against automatically. This closes that gap:

- **`tests/`** - a real pytest suite (73 tests including the To-Do.md instrumentation tests below, runs in well under a second, no network required) covering every deterministic piece of logic: IP/CIDR validation (including the exception-chaining fix below), SIP packet templating (including the CRLF-injection fix and F13's per-instance `client_port`), CLI argument parsing (including F15's `type=int` fixes), the thread pool (including a direct regression test that `queue.join()` genuinely blocks for slow workers - F4), and a static structural check on every `.message` template that would have caught F21 automatically (verified live: reintroducing F21's exact bug into `bye.message` makes the relevant test fail immediately). Network-dependent behavior (does SIP-NES actually find a live host) is intentionally out of scope for this suite and continues to rely on the manual methodology in [Testing](#testing) - mocking sockets/Scapy convincingly enough to be worth trusting was judged not worth it for a first suite.
- **`pyproject.toml`** - `ruff` and `pytest` configuration. This is a config-only file (no `[project]`/`[build-system]` table), so it does **not** reintroduce pip-installability, which was a deliberate earlier decision (see [Upgrade notes](#upgrade-notes)).
- **`.github/workflows/ci.yml`** - runs `ruff check`, the full pytest suite, and a `--help` smoke test on every push/PR, across Python 3.9/3.11/3.13. The version matrix specifically targets what F12 was about: the tool broke against a newer Scapy release with zero warning, and testing across Python versions is a cheap way to catch that class of drift before a user hits it instead of after.
- A full `ruff` pass across `src/` surfaced zero unused-import/unused-variable/undefined-name findings (clean already) and a handful of legitimate style/exception-handling issues, all fixed: f-string modernization, `dict()` → `{}` literals, and - the one with actual behavioral value - `raise ... from None` in 4 places where an internal exception (a `netifaces` lookup failure, an `int()` parse failure) is deliberately translated into a clean, user-facing CLI error. Previously the original low-level exception's traceback would still print as "During handling of the above exception, another exception occurred", which is just noise for a CLI tool's error output.

## Architecture changes

- **Package layout**: the three top-level scripts (`mr.sip.py`, `sip_packet.py`, `utilities.py`) are now a proper package under `src/`, with `mr.sip.py` reduced to a two-line runner (`from src.cli import main`). No `pip install` step was introduced or removed — it is still invoked as `python3 mr.sip.py ...`, matching the free/open-source pentest-tool convention (clone and run in place).
  - `src/core/` — `errors.py`, `logging_config.py`, `net_utils.py`, `sip_packet.py`, `threadpool.py`
  - `src/modules/` — `nes.py`, `enum.py`, `das.py` (one `run(args, ...)` each, no module-level global state)
  - `src/data/method/*.message`, `src/data/wordlists/*.txt` — resolved relative to the package's own location (`Path(__file__)`), not the current working directory, so the tool works correctly regardless of which directory it's run from.
- **Logging**: `src/core/logging_config.py` replaces raw `print()` with the stdlib `logging` module — colored console output (`--verbose` now actually toggles DEBUG; previously a no-op), plus a plain-text, ANSI-stripped log file per run (`logs/mrsip_<timestamp>.log`), useful as pentest evidence/report material.
- **Error handling**: `src/core/errors.py` introduces a typed exception hierarchy (`MrSipError` base; `PacketSendError`, `TemplateNotFoundError`, `InvalidInterfaceError`) caught once, centrally, in `cli.main()`. Replaces scattered `exit(0)` calls and ad-hoc `{"status": False}` dict returns that callers didn't always check (see F1).
- **Shared thread pool**: `src/core/threadpool.py`'s `run_worker_pool()` is now used by both SIP-NES and SIP-ENUM, replacing duplicated per-module thread-setup code. Fixes F2 and F4 (below) in one place instead of two.
- **Output layout**: `output/ip_list.txt` (SIP-NES's output / SIP-ENUM's default input) and `output/from.txt` moved out of the repo root into `output/`; `badges/`, `logo/`, `screenshots/` consolidated into `assets/`.

## Correctness fixes

Each entry is: what was wrong, why it's wrong (RFC citation where applicable), and how it was fixed. RFC 3261 section numbers were verified against the actual RFC text in `docs/rfcs/`, not general knowledge.

- **F3 — SIP-NES `--mt=register`/`subscribe` sent an invalid Request-URI.** `register.message`'s `To:`/Request-URI both used `sip:[[to_user]]@server`, but the old code blanked `to_user` for register/subscribe ("toUser should be omitted" — original author's own comment), producing `sip:@server`, silently dropped by Asterisk. RFC 3261 §10.3 step 5 says the registrar reads the AOR from the `To:` header, not the Request-URI, and §10.2 says the Request-URI for REGISTER **MUST NOT** contain userinfo. Fixed in two parts: `to_user` is now paired with `from_user` (same identity) instead of blanked, and the Request-URI itself was changed to the bare `sip:[[server_ip]]` form.
- **F7 — CANCEL template's `CSeq` used the wrong method.** `cancel.message` had `CSeq: 1 INVITE`; RFC 3261 §9.1 requires the CSeq method to match the request line (`CANCEL`). Fixed.
- **F13 — `client_port` was a class attribute, not an instance attribute.** `sip_packet.client_port = random.randint(...)` was assigned once at class-body scope (present since the 2017 initial commit — confirmed via `git show`), so **every packet sent by the process for its entire lifetime shared the same source port**, regardless of target or message type. The port baked into each packet's `Via`/`Contact` headers therefore didn't match the actual UDP source port for most packets. Fixed by moving the assignment into `__init__`.
- **F21 — unclosed `<` in the `From:` header of `bye.message`/`cancel.message`/`sp-invite.message`.** `From: [[from_user]] <sip:[[from_user]]@[[client_ip]];tag=[[tag_value]]` never closes the angle bracket, so `;tag=` ends up *inside* the URI instead of as a header parameter after `>` — invalid per RFC 3261 §25.1's `name-addr` grammar. Present since the 2017 initial commit. Caught by inspecting the *target* Asterisk server's own logs (`PJSIP syntax error exception when parsing 'From' header`) — invisible from the client side because SIP-DAS's flood mode never waits for a response. Fixed by adding the missing `>`; verified afterward that `sp-invite` gets a real `401` instead of being silently dropped.
- **Socket send used the wrong source port.** The plain-socket (`-l`) sender called `connect()` without first `bind()`-ing to `client_port`; for a UDP socket this means the kernel picks a random ephemeral source port, so (same symptom as F13, different cause) the actual packet's source port didn't match what the SIP message claimed in `Via`/`Contact`. Fixed by binding to `client_port` before `connect()`.
- **`check_ip_address` range validator only checked the first IP.** An indentation bug meant the second IP in an `a.b.c.d-w.x.y.z` range argument was never actually validated. Fixed.
- **Client/Server misclassification when writing `output/ip_list.txt`.** A copy-paste bug (present since the Python 3 upgrade commit) wrote the literal string `"SIP Server"` even in the branch handling hosts classified as `"SIP Client"`. Fixed.
- **NES crashed on a single target with a multi-entry `--from`/`--to` wordlist.** `target_networks` was only assigned in the `-`(range)/`/`(CIDR) branches, but the "custom wordlist" branch reused the same threaded code path and referenced it, raising `UnboundLocalError`. Found while reproducing baseline behavior for this changelog; already fixed as an incidental side effect of the restructure (`nes.py` now always assigns `target_networks` before it's used).

## Security fixes

- **CRLF / SIP header injection via wordlist-driven fields.** `fill_packet_data()` substituted `--from`/`--to`/`--ua`/`--su` values directly into the message template with no sanitization; a value containing `\r\n` could inject arbitrary extra headers or body content. Checked the tool's own design intent first (original 2017 README: messages are meant to be "grammatically compatible with SIP RFCs" specifically to evade anomaly detection) — injection capability was never an intended feature. Fixed by stripping `\r`/`\n` from every substituted value before it's written into the packet.
- **SIP-DAS's Scapy/spoofing modes attempted to run without root and failed deep inside the send path.** Now checks `os.geteuid() == 0` up front and exits with one clean, actionable message ("...requires root privileges. Please run with sudo or use the -l flag.") instead of a raw traceback (or, pre-F1, a false "success").
- **`threadpool.prompt_yes_no()` — the "N packages will be generated, continue? (y/n)" gate SIP-NES/SIP-ENUM show before a large cross-product run (the same gate F20 is about) — defaulted to "yes" when stdin was unavailable** (`EOFError`, e.g. run from a script, cron job, or CI). This was introduced during this session's own restructure, not inherited from upstream: the original code called bare `input()` with no exception handling, so a non-interactive run simply crashed - "does nothing" is a safer failure mode than "silently sends every packet without anyone confirming." Also inconsistent with the function's own next branch, where an unrecognized typed answer already defaults to `False` (abort). Fixed: `EOFError` now also returns `False`. Found via live testing against a real Docker/Asterisk target from a non-interactive shell — reproduced the silent-proceed behavior firsthand, then fixed it and re-verified the same command now aborts instead.

## Reliability / concurrency fixes

- **F1 — SIP-DAS reported success even when zero packets were actually sent.** `dosSmilator()` (now `das.py`) never checked `generate_packet()`'s return value; running an unprivileged Scapy/spoofing scenario reported `"N packet sent"` while every single send silently failed with a permission error. Reproduced live against the true baseline: `-r` mode with no root printed `"3 packet sent"` with **zero** actual packets on the wire. Fixed: `generate_packet()` now raises typed exceptions on failure, and `das.py` tracks and reports actual sent/failed counts plus the last error.
- **F2 — concurrent worker output could interleave into merged, unparseable log lines.** Raw concurrent `print()` calls from SIP-NES/SIP-ENUM's worker threads could interleave mid-line under load. Proven with a live 9,000-concurrent-result stress test: **58 merged lines** under the old code vs. **0** after the fix. Fixed by routing all worker output through `logging`, whose handlers serialize writes internally.
- **F4 — busy-wait race condition in the thread pool.** `while not workQueue.empty(): pass` doesn't guarantee every item has actually finished processing when the loop exits — a classic TOCTOU race, and it burns a full CPU core spinning. Fixed with `queue.join()`/`task_done()`, which blocks until every item is genuinely done.
- **`output/ip_list.txt` write corruption under concurrent workers.** Multiple NES/ENUM worker threads could interleave writes to the same output file. Fixed with a `threading.Lock()` around the write + dedup step.

## Performance fixes

- **F8 — SIP-DAS re-read all four wordlist files from disk on every single loop iteration.** At the default flood counter (~1×10⁸), this is ~4×10⁸ redundant file opens. Fixed: wordlists are read once, up front.
- **F16 — `defineTargetType()` re-read `servers.txt` from disk on every single successful scan result**, same class of bug as F8 but on the SIP-NES side. Fixed with a module-level cache.
- **F20 — SIP-NES's default invocation queued up to 81,000,000 requests against a single host.** `--from`/`--to` default to the bundled 9,000-line wordlists (sized for SIP-ENUM/SIP-DAS); for any message type other than `register`/`subscribe`, SIP-NES cross-products them (`itertools.product`). The tool's own simplest documented usage — `--nes --tn=<ip> --mt=options`, no `--from`/`--to` override — therefore queued 9,000 × 9,000 requests. **This mechanism itself is intentional**, confirmed via `git log -S` against the original author's own code comment (`# both fromUser and toUser should be accepted` — identity-aware probing for operators who supply their own `--from`/`--to`), so it was not removed. The bug was the *default* wordlist size being inherited from SIP-ENUM/SIP-DAS. Fixed: when `--from`/`--to` are left at their default path (i.e., not explicitly overridden) and the message type isn't register/subscribe, SIP-NES sends a single generic probe per target instead of the full cross-product. Passing an explicit `--from`/`--to` of any size still gets the original, fully-intended cross-product behavior.

## Usability / error-handling fixes

- **F9 — `promisc()` unconditionally shelled out to `ip link set ... promisc ...`**, which is Linux-only syntax; on macOS/BSD this just failed with a misleading "you must run as root" warning even as root. Gated to Linux, with a clear debug-level explanation elsewhere (doesn't affect the actual spoofed-send capability, which doesn't depend on promiscuous mode).
- **F11 — dead code.** Unused SIP status-code class constants removed from `sip_packet.py`.
- **F12 — unpinned dependencies.** `requirements.txt` had no version bounds; this is exactly how the tool ended up broken against current Scapy (`conf.iface` changed from a `str` to a `NetworkInterface` object between versions — the very first thing that had to be patched to get the baseline running for this changelog's comparison). Pinned to tested ranges.
- **F14 — SIP-DAS's `-m` (manual spoof) mode couldn't parse its own documented input.** `--il` is documented (usage text, README) as accepting SIP-NES's `output/ip_list.txt`, which is `ip;user_agent;type` per line — but `-m` mode read each line as a raw IP, producing a confusing DNS-lookup-style error. Fixed to take the first `;`-delimited field, which also still works for a plain one-IP-per-line file.
- **F15 — `-c`/`--count` had no `type=int` validation** (unlike `--dp`/`--tc`, which do); an invalid value crashed with a raw traceback inside `das.py` instead of a clean CLI error. Fixed.
- **F17 — stray local `import os`** inside `cli.main()` instead of at module top. Fixed.
- **F18 — wordlist/manual-IP files opened with a bare `open()`** left to be closed by CPython refcounting rather than an explicit `with` block. Fixed.
- **F19 — overly broad `except Exception` in `sip_packet.generate_packet()`.** Wrapped *any* exception — including real bugs like `TypeError`/`AttributeError` — as `PacketSendError`, which could silently mask programming errors as ordinary "packet send failed" outcomes. Narrowed to `except (OSError, UnicodeDecodeError)`, the actual failure modes a send/receive cycle can raise; anything else now surfaces as a real traceback. Verified with two targeted tests: a genuine `ValueError` (corrupted `client_port`) now propagates correctly, while a genuine network timeout still becomes a clean `PacketSendError`.
- **CIDR range widened from `/24`-only to `/8`–`/32`,** and a related bug fixed where `/31`/`/32` scans found zero hosts (`ipaddress.hosts()` is empty for those masks) — now falls back to scanning every address in the network for those cases.
- **A missing `--from`/`--to`/`--su`/`--ua`/`--il` wordlist file raised a raw `FileNotFoundError` traceback** in all three modules (NES, ENUM, DAS all read these with a bare `open()`), instead of the clean CLI error every other invalid-input case already gets. Caught centrally in `cli.main()`, next to the existing `MrSipError` handler, so all three modules get the fix from one place. Found via live testing against a real Docker/Asterisk target with an intentionally-missing wordlist path.
- **SIP-DAS's `-r`/`-s`/`-m` (IP spoofing) silently did nothing when combined with `-l` (socket library mode)**, since spoofing requires raw Scapy packets that `-l` explicitly opts out of - no error, no warning, just a normal-looking run that never actually spoofed anything. Now logs a warning up front telling the user to drop `-l` if they actually want the spoofing to take effect. Found via live testing: `-m --il=... -l` "succeeded" while silently ignoring the manual IP list.
- **The "N packages will be generated, continue? (y/n)" confirmation prompt required pressing Enter after y/n**, because it used `input()`, which is line-buffered. Replaced with a single-keypress read via `termios`/`tty` on POSIX (falls back to line input elsewhere). First attempt used raw mode (`tty.setraw`), which turned out to be the wrong tool here - raw mode also disables terminal echo *and* output post-processing (CR/LF translation), which produced doubled/misaligned output on a real terminal (confirmed live). Switched to cbreak mode (`tty.setcbreak`), which only disables line buffering and leaves echo/output processing alone - the keypress is echoed once by the terminal itself, and a stray buffered byte (e.g. a leftover Enter from habit) is explicitly flushed before reading so it can't bleed into the next prompt. If stdin isn't an interactive TTY (piped, scripted, CI), it still raises the same `EOFError` the fail-safe fix above already handles - refuses to proceed rather than guessing. Note this is a behavior change for any script that used to auto-confirm via `echo y | mr.sip.py ...`: piped input is no longer treated as a real "yes," since single-keypress reads require an actual TTY.
- **SIP-DAS's progress bar was a hand-rolled `\r`-based implementation** (`net_utils.printProgressBar()`) that didn't reliably overwrite its own line outside a raw interactive TTY (e.g. when output is piped or copy-pasted from scrollback), and used `end=" "` instead of `end=""`, leaving a stray trailing space. Replaced with `tqdm` (new dependency, pinned `>=4.60,<5`), which handles single-line overwriting correctly across terminals/redirection and reports ETA/rate for free.
- **Scapy mode's `send(pkt, iface=conf.iface)` call was passing a dead parameter.** Confirmed against Scapy's own source (`sendrecv.py`): `send()` is an L3 call that picks the outgoing interface from the routing table, and explicitly deletes an `iface` kwarg after emitting a `SyntaxWarning` - the parameter has never done anything here. It was also spamming one `"Sent 1 packets."` line per packet (Scapy's default `verbose=True`), which broke the progress bar's single-line rendering. Removed the dead `iface` kwarg and added `verbose=False`. `--if`/`--interface` is unaffected - it still works via the global `conf.iface` that Scapy's own routing-table lookup already consults (set in `cli.py`), which was never the thing this dead per-call kwarg controlled.

## Known limitation left open

- **F6 — SIP-ENUM's 401/403 = "extension exists" heuristic.** Produces false positives against servers that reject *every* unmatched request the same way regardless of the extension (e.g. modern PJSIP's default `endpoint_identifier_order` behavior). RFC 3261 doesn't directly support or refute the heuristic either way; a real fix needs a broader redesign of how SIP-ENUM tells "exists but needs auth" apart from "rejected regardless," so it was left open rather than patched superficially. **Independently confirmed to be an environment-specific limitation, not a broken heuristic**: tested against a real chan_sip-based Asterisk (Trixbox CE 2.8, via QEMU) with 3 real extensions (one secretless, two with a secret) plus one nonexistent extension — SIP-ENUM reported exactly 3/3 real extensions with the correct auth-required/not-required split, and zero false positives for the nonexistent one. The heuristic works exactly as originally designed against classic chan_sip; it only breaks against PJSIP. **Correction (1.6.4/F45): "it only breaks against PJSIP" turned out to be incomplete, not just imprecise** - modern `chan_sip` (any Asterisk since 1.8, ~2011) blanket-rejects too, by default, via `alwaysauthreject`; Trixbox CE's clean result here held because it predates that default, not because it's `chan_sip` specifically. See the `1.6.4` section above for the source-verified explanation (Asterisk's own source across multiple version branches, cross-referenced against AST-2013-003/CVE-2011-2666) - left this entry's original wording otherwise unchanged as a record of what was understood at the time.

## Testing

Three independent verification passes, against two structurally different SIP stacks:

1. **Docker / Asterisk 22 (PJSIP)** — `andrius/asterisk:latest`, 3 PJSIP endpoints, digest auth. Two full regression rounds covering all three modules (single host, IP range, CIDR; small and full 9,000-entry wordlists; all 7 message types; all 3 spoofing modes; socket and Scapy protocols). The second round included a live 9,000-concurrent-result stress test proving F2's fix (0 merged log lines vs. 58 previously).
2. **QEMU / Trixbox CE 2.8 (classic chan_sip, Asterisk 1.6)** — a real third-party lab image provided outside this repo, running under full software emulation (`qemu-system-x86_64 -M pc -accel tcg -cpu qemu64`, since there's no hardware virtualization for x86 on the Apple Silicon host it ran on). Used specifically to test SIP-ENUM's heuristic against a structurally different (non-PJSIP) target, per the F6 note above.
3. **Baseline-vs-current comparison** (this session) — the true pre-refactor upstream code (commit `bdd98ad`) was checked out into an isolated `git worktree` and run against the same Docker target as the current code, scenario-for-scenario. See the next section.

## Baseline vs. current: reproducible comparison

Baseline = commit `bdd98ad` (pre-refactor upstream), patched with the one-line Scapy compat shim (`str(conf.iface)`) it needs just to launch against a modern Scapy install — otherwise it crashes immediately with `TypeError: argument 1 must be str, not NetworkInterface`, unrelated to any logic in this changelog. Both versions were run against the same live Docker/Asterisk target.

| Scenario | Baseline (`bdd98ad`) | Current |
|---|---|---|
| Launch with a modern Scapy install | ❌ Crashes (`TypeError`) without a manual patch | ✅ Works out of the box |
| SIP-NES, single host + multi-entry `--from`/`--to` | ❌ Crashes (`UnboundLocalError: target_networks`) | ✅ Correct (16/16 expected results) |
| SIP-NES `--mt=register` | ❌ 0 results, 5s timeout (F3) | ✅ 1/1 correct, <0.01s |
| SIP-DAS, socket mode | ✅ Correct | ✅ Correct — no behavior change |
| SIP-DAS, unprivileged Scapy `-r` | ❌ Reports `"3 packet sent"`; **0** actually sent (F1) | ✅ Clean upfront error, no false report |
| SIP-ENUM, 401/403 heuristic | Same false positive on a nonexistent extension | Same — **unchanged on purpose** (F6, documented, not a regression) |

## Upgrade notes

- Invocation is unchanged: `python3 mr.sip.py --nes|--enum|--das ...`. No `pip install` step, before or after.
- `output/ip_list.txt` and `output/from.txt` replace the old repo-root `ip_list.txt`/`from.txt` paths; `-i/--ip-save-list` now defaults to `output/ip_list.txt`.
- If anything outside this repo imported the old flat modules directly (`import sip_packet`, `import utilities`, etc.) rather than going through `mr.sip.py`, those import paths no longer exist — the equivalents now live under `src.core`/`src.modules`. The CLI surface itself did not change.
- `--verbose`/`-v` now actually does something (enables DEBUG-level logging); previously it was a no-op.
- `README.md` had a few stale technical claims fixed to match current behavior: a "Mr.SIP **Pro** Installation" heading that actually described installing the public tool, a documented `python3 mr.sip.py –usage` invocation that isn't a real flag, a "default interface is eth0" claim (it's whatever Scapy's `conf.iface` auto-detects), and a general-usage example listing Pro-only flags (`--sniff`, `--crack`, `--vscan`, ...) as if they existed in this repo. Mr.SIP Pro's own marketing/feature copy elsewhere in the README was left untouched - that's the vendor's content, not a technical-accuracy issue with this repo's code.
